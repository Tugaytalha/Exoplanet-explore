# XGBoost KOI Disposition Prediction
# Requirements for training and inference

# Core ML libraries
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
xgboost>=1.5.0

# API Framework
fastapi>=0.105.0
uvicorn[standard]>=0.27.0

# Performance optimization (HIGHLY RECOMMENDED)
orjson>=3.6.0  # Fast JSON serialization (5-10x faster than standard json)
polars>=0.19.0  # Ultra-fast DataFrame library (5-100x faster than pandas)
pyarrow>=15.0.0  # Required by polars for faster CSV reading

# Visualization
matplotlib>=3.4.0
seaborn>=0.11.0

# Model persistence
joblib>=1.1.0

# Optional but recommended
imbalanced-learn>=0.9.0
shap>=0.41.0

